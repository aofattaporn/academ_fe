export type ProjectType = {
    projectName: string;
    projectId: string;
  };

export type ClassType = {
  className: string;
  classId: string;
};

export type MytaskType  = {
  taskName: string;
  taskId: string;
  taskDuedate: string;
  taskFromproject: string;
};